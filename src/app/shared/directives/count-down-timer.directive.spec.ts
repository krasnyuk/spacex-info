import { CountDownTimerDirective } from './count-down-timer.directive';

describe('CountDownTimerDirective', () => {
  it('should create an instance', () => {
    const directive = new CountDownTimerDirective();
    expect(directive).toBeTruthy();
  });
});
